version: "3.9"

services:
  backend:
    build: ./backend
    container_name: bytebites-backend
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=bytebites-dev-secret
    restart: unless-stopped
